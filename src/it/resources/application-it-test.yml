# --------------------------------------------------------
# Properties for unit tests.  Tests are run with an
# active profile of 'test'
# --------------------------------------------------------
security:
  basic:
    enabled: "false"

spring:
  session:
    enabled: "false"

gitlab:
  enabled: "false"